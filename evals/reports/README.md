# Generated reports

Eval reports are generated artifacts. Keep only this policy file and the
ignore rules in version control; persist reviewed promotion evidence through a
sanitized baseline definition under `evals/baselines/`.
